#include "main.h"
/**
 * print_alphabet - function that print all alphabet in lowercase
 *
 * Return: always 0;
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(letter);

	_putchar('\n');
}
